<%= t ".you_have_unread" %>

<% @user.unread_files.each do |file| %>
* <%= file["title"].html_safe %>
  <%= t ".mod_at_by", :date => l(file["modifiedDate"]), :user => file["lastModifyingUserName"] %>
  <%= file["alternateLink"].html_safe %>

<% end %>

 ------------------------------------------------------------------
/ Google Drive Notifier / <%= root_path(:only_path => false) %>
